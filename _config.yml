# Site settings
title: "Tyler Walters - Resume"
description: "Multi-media resume for Tyler Walters."

# Build settings
markdown: kramdown
sass:
  sass_dir: _sass
  style: compressed

# Design settings
resume_theme: default

# Resume content is now in _data/ directory
# All resume data has been migrated to:
# - _data/profile.yml (name, title, contact, summary, social links)
# - _data/experiences.yml (current experience)
# - _data/employment.yml (prior employment)
# - _data/consulting.yml (consulting experience)
# - _data/clients.yml (client list)
# - _data/educations.yml (education)
# - _data/projects.yml (side projects)
# - _data/skills.yml (skills)
# - _data/certifications.yml (certifications)
# - _data/publications.yml (publications)
# - _data/recognitions.yml (recognitions)
# - _data/interests.yml (interests)
# - _data/sections.yml (section visibility)
#
# This allows editing content without restarting the Jekyll server!
